import styled from "styled-components";
import { layout } from "../../constants/layout";

export const CommonLayout = styled.div`
    padding: ${layout.VERTICAL_LAYOUT}px ${layout.HORIZONTAL_LAYOUT}px
`