import COLOR from "@/constants/color";
import styled from "styled-components";

export const ReviewDetailTitleContainer = styled.div`
    border-top: 1px solid ${COLOR.defalutColor};
    border-bottom: 1px solid ${COLOR.gray};
    padding: 15px 0px ;
`